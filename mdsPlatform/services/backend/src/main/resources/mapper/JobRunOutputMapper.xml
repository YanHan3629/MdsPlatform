<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.fwdrobo.sirius.mapper.JobRunOutputMapper">

    <insert id="insert">
        INSERT INTO public.job_run_output
        (run_id, job_id, output_repo_id, output_commit_id, output_order, params)
        VALUES
            (#{runId},
             #{jobId},
             #{repoId},
             #{commitId},
             #{order},
             CAST(#{paramsJson} AS jsonb))
    </insert>

</mapper>
